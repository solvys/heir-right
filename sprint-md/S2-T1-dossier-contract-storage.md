# Sprint Brief: S2-T1 -- Dossier Contract + Storage

Owner: TP  
Parent: `S2-ORCH`  
Dependencies: S1

Finalize and build the shared raw dossier/data contracts and local persistence outputs.

Acceptance:

- `SourceFact`, `RawDossier`, `DossierClaim`, `DossierEvent`, `CrmAdapter`, and `DocumentPacket` compile from `@ple/types`.
- Worker writes local run/dossier/sync/document outputs.
